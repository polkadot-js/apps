language: node_js
node_js:
  - "10"
cache:
  yarn: true
  directories:
    - node_modules
before_install:
  - yarn global install lerna yarn
install:
  - yarn install
script:
  - yarn polkadot-dev-build-travis
