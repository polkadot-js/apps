name: Chain
on: [pull_request]

jobs:
  urls:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      run: |
        yarn install --immutable | grep -v 'YN0013'
        yarn ci:chainUrls
